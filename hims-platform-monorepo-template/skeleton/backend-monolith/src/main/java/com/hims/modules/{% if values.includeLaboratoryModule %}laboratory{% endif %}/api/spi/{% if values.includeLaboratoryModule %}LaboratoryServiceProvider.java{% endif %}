package com.hims.modules.laboratory.api.spi;

import java.util.UUID;

/**
 * Service Provider Interface for Laboratory module.
 * 
 * Pattern: Laboratory Module (LIMS)
 */
public interface LaboratoryServiceProvider {
    
    // Placeholder - add actual service methods here
    // Methods should use DTOs from api.dto package
    
    // Example methods (to be implemented):
    // - createSample(CreateSampleDto dto)
    // - getSampleByBarcode(String barcode)
    // - updateQcStatus(UUID sampleId, String status)
    // - sendToMachine(UUID sampleId, String machineId)
}

