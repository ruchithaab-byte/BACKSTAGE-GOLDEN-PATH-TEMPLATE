package com.hims.modules.integration.internal.listener;

import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Component;

@Component
public class IntegrationEventListener {
    @KafkaListener(topics = "integration.hl7.message.received", groupId = "integration-service")
    public void handleHl7MessageReceived(String message) { /* Placeholder */ }
}

